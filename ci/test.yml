---
platform: linux
image_resource:
  type: registry-image
  source:
    repository: taylorsilva/dcind # docker in docker
inputs:
  - name: dremio-demo
run:
  path: bash
  args:
    - -cex
    - |
      docker run -i thekubeguru/dremio-demo:latest poetry run python3 server.py test